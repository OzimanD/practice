package Sergey.OOP.task20;
//испорт библиотеки массов далее
import java.util.ArrayList;
//создается интерфейс далее
public interface StudInter {
    //создаем тип данных ArrayList<String> с именем al_ln
    // в который передаем паарметры (Students[] student) далее
    ArrayList<String> al_ln(Students[] student);
    //создаем метод в который передаем (Students[] students) далее
    int i_kolvo (Students[] students);
}
